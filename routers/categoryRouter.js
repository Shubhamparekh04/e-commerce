// const { Router } = require("express");
// const categoryRouter = Router();
// const categoryController = require("../controllers/categoryController");
// const upload = require("../middleware/imageUpload");

// categoryRouter.post("/createCat", upload, categoryController.create);
// categoryRouter.get("/getCat", categoryController.get);
// categoryRouter.delete("/delCat/:id", categoryController.del);
// categoryRouter.patch("/updateCat/:id",upload,categoryController.update);

// module.exports = categoryRouter;
